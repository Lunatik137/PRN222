name: 'Test App'
description: 'Builds and tests .NET project'

runs:
  using: "composite"
  steps:
    - name: Restore dependencies
      run: dotnet restore
      shell: bash
      working-directory: './Project_Group3'
    
    - name: Build
      run: dotnet build --no-restore
      shell: bash
      working-directory: './Project_Group3'

    - name: Test
      run: dotnet test --no-build --verbosity normal
      shell: bash
      working-directory: './Project_Group3'
